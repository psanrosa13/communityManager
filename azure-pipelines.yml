# Maven
# Build your Java project and run tests with Apache Maven.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/java

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Maven@3
  inputs:
    mavenPomFile: 'pom.xml'
    mavenOptions: '-Xmx3072m'
    javaHomeOption: 'JDKVersion'
    jdkVersionOption: '1.8'
    jdkArchitectureOption: 'x64'
    publishJUnitResults: true
    testResultsFiles: '**/surefire-reports/TEST-*.xml'
    goals: 'package'

- task: AzureWebApp@1
  inputs:
    azureSubscription: 'Visual Studio Enterprise (30c5c3f3-d2c7-4dc0-92ce-79b9ed1d5a14)'
    appType: 'webAppLinux'
    appName: 'communitymanagerapp'
    deployToSlotOrASE: true
    slotName: 'production'
    package: '$(System.DefaultWorkingDirectory)/**/*.jar'
    runtimeStack: 'TOMCAT|9.0-jre8'
    startUpCommand: 'java -jar $(appName).jar'